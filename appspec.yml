version: 0.0
os: ubuntu
files:
  - source: /
    destination: /home/ubuntu/server
    overwrite: yes
permissions:
  - object: /home/ubuntu/server
    owner: root
    group: root
    mode: 755
hooks:
  AfterInstall:
    - location: deploy.sh
      timeout: 60
      runas: root
